# gitops/

Flux-reconciled cluster state: `clusters/` (entry points),
`infrastructure/` (cluster add-ons), `apps/` (workloads). All content
here is owned by Flux after bootstrap.
See [ADR-0002](../docs/adr/0002-repository-structure.md). Layer 4+, pending.
