---
# bootstrap/roles/kube_bootstrap/tasks/join.yml
#
# Joins worker nodes to the cluster.
# Pattern: generate join command on the CP host via
#   kubeadm token create --print-join-command --ttl {{ kube_token_ttl }}
# then execute it on each worker via hostvars delegation.
#
# Written here; exercised first in edge-site shape (not lab-single-node).
# For ha-bare-metal CP join (--control-plane --certificate-key) is a
# separate extension — deferred.
#
# TODO (slice H): implement token generation + join task.
